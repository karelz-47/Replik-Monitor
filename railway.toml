[build]
builder = "DOCKERFILE"

[deploy]
preDeployCommand = "python -m replik_monitor.cli migrate"
healthcheckPath = "/healthz"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
